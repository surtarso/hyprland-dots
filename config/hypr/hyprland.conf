# ArchRiot Hyprland Main Configuration
# -----------------------------------------------------------------------------
# This file is your main Hyprland config:
#   ~/.config/hypr/hyprland.conf
#
# Purpose:
# - Allows ArchRiot installer to patch Hyprland with fixes and updates.
# - Does NOT persist across installs/repairs (the installer REPLACES this file!)
#
# Usage:
# - DO NOT EDIT THIS FILE! IT WILL BE OVERWRITTEN!
# - For custom edits use the provided user persisted files:
#   - monitors.conf     -> Configure monitor/workspaces, tablets, custom env's and auto-starts
#   - windowrules.conf  -> Configure non-default apps window rules
#   - keybindinds.conf  -> Configure and/or overwrite custom keybindings and default applications
# - Reload Hyprland to apply changes:
#     hyprctl reload
#
# Notes:
# - User persisted files contain documentation on how to use them.
# - If you make changes to this file, revert it first before opening any issues.
# - Prefer consistency and minimal overlap with system defaults
# - Learn how to configure Hyprland: https://wiki.hyprland.org/Configuring/
# -----------------------------------------------------------------------------

# ----------------------------------------------------------------------------
# Change your personal monitor setup in here to keep the main config portable
source = ~/.config/hypr/monitors.conf
# Do not move this line or type any code before it.
# ----------------------------------------------------------------------------

# ==================== CORE HYPRLAND CONFIG ====================
# Default applications
$terminal = ghostty
$fileManager = Thunar
$browser = archriot-brave
$messenger = env GDK_SCALE=1 signal-desktop --ozone-platform=wayland --enable-features=UseOzonePlatform
$webapp = $browser --force-device-scale-factor=1 --high-dpi-support=1 --app

# Basic variables
$mod = SUPER

# Environment variables
env = XCURSOR_THEME,Bibata-Modern-Ice
env = HYPRCURSOR_THEME,Bibata-Modern-Ice
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

# Prefer dark themes on all gnome apps
exec-once = gsettings set org.gnome.desktop.interface color-scheme 'prefer-dark'

# Autostart applications
exec-once = $HOME/.local/share/archriot/install/archriot --waybar-launch
exec-once = mako
exec-once = /usr/bin/hypridle
# # exec-once = hyprsunset -t 3500  # Disabled via Control Panel  # Disabled via Control Panel
exec-once = kanshi

exec-once = $HOME/.local/share/archriot/install/archriot --startup-background
exec-once = sleep 2 && $HOME/.local/share/archriot/install/archriot --welcome

# Start Mullvad GUI minimized to tray if account is logged in
exec-once = $HOME/.local/share/archriot/install/archriot --mullvad-startup

# Make sure binds are up-to-date on every save/reload
exec = $HOME/.local/share/archriot/install/archriot --help-binds-generate

# ========================= WINDOW RULES =======================
# Window rules
windowrule = scrolltouchpad 1.5, class:^(com.mitchellh.ghostty)$
windowrulev2 = float, class:^(org\.pulseaudio\.pavucontrol)$
windowrulev2 = size 800 85%, class:^(org\.pulseaudio\.pavucontrol)$
windowrulev2 = center, class:^(org\.pulseaudio\.pavucontrol)$
windowrulev2 = float, class:^(blueberry.py)$
windowrulev2 = float, class:^(org.gnome.Calculator)$

# X (Twitter) window rules
windowrulev2 = float, class:^(brave-x\.com.*)$
windowrulev2 = size 40% 85%, class:^(brave-x\.com.*)$
windowrulev2 = move 58% 8%, class:^(brave-x\.com.*)$

# X (Twitter) specific class rules (for fuzzel launches)
windowrulev2 = float, class:^(twitter-x)$
windowrulev2 = size 40% 85%, class:^(twitter-x)$
windowrulev2 = move 58% 8%, class:^(twitter-x)$

# X (Twitter) fallback rules for brave-browser class
windowrulev2 = float, class:^(brave-browser)$, title:.*X.*
windowrulev2 = size 40% 85%, class:^(brave-browser)$, title:.*X.*
windowrulev2 = move 58% 8%, class:^(brave-browser)$, title:.*X.*

# Telegram window rules
windowrulev2 = float, class:^(org\.telegram\.desktop)$
windowrulev2 = float, class:^(telegram-desktop)$
windowrulev2 = size 40% 85%, class:^(org\.telegram\.desktop)$
windowrulev2 = size 40% 85%, class:^(telegram-desktop)$
windowrulev2 = move 58% 8%, class:^(org\.telegram\.desktop)$
windowrulev2 = move 58% 8%, class:^(telegram-desktop)$

# Bluesky PWA window rules
windowrulev2 = float, class:^(bluesky)$
windowrulev2 = size 40% 85%, class:^(bluesky)$
windowrulev2 = move 58% 8%, class:^(bluesky)$

# ArchRiot.org PWA window rules
windowrulev2 = float, class:^(archriot-help)$
windowrulev2 = size 1178 85%, class:^(archriot-help)$
windowrulev2 = center, class:^(archriot-help)$

windowrulev2 = float, class:^(brave-archriot\.org.*)$
windowrulev2 = size 1024 85%, class:^(brave-archriot\.org.*)$
windowrulev2 = center, class:^(brave-archriot\.org.*)$

# Google Messages window rules
windowrulev2 = float, class:^(brave-messages\.google\.com.*)$
windowrulev2 = size 40% 85%, class:^(brave-messages\.google\.com.*)$
windowrulev2 = center, class:^(brave-messages\.google\.com.*)$

# Google Messages fallback rules for brave-browser class
windowrulev2 = float, class:^(brave-browser)$, title:.*[Mm]essages.*
windowrulev2 = size 40% 85%, class:^(brave-browser)$, title:.*[Mm]essages.*
windowrulev2 = center, class:^(brave-browser)$, title:.*[Mm]essages.*

# Proton Mail fallback rules for brave-browser class
windowrulev2 = float, class:^(brave-mail\.proton\.me.*)$
windowrulev2 = size 45% 85%, class:^(brave-mail\.proton\.me.*)$
windowrulev2 = move 52% 8%, class:^(brave-mail\.proton\.me.*)$
windowrulev2 = float, class:^(brave-browser)$, title:.*[Pp]roton.*[Mm]ail.*
windowrulev2 = size 45% 85%, class:^(brave-browser)$, title:.*[Pp]roton.*[Mm]ail.*
windowrulev2 = move 52% 8%, class:^(brave-browser)$, title:.*[Pp]roton.*[Mm]ail.*
windowrulev2 = move 52% 8%, class:^(brave-.*)$, title:.*[Pp]roton.*[Mm]ail.*

# Signal window rules
windowrulev2 = float, class:^(Signal)$
windowrulev2 = float, class:^(signal)$
windowrulev2 = size 40% 85%, class:^(Signal)$
windowrulev2 = size 40% 85%, class:^(signal)$
windowrulev2 = move 2% 8%, class:^(Signal)$
windowrulev2 = move 2% 8%, class:^(signal)$

# Claws Mail window rules
windowrulev2 = float, class:^(claws-mail)$
windowrulev2 = size 1200 85%, class:^(claws-mail)$
windowrulev2 = move 2% 8%, class:^(claws-mail)$

# Feather wallet window rules
windowrulev2 = float, class:^(feather)$
windowrulev2 = size 50% 50%, class:^(feather)$
windowrulev2 = center, class:^(feather)$

# Trezor Suite window rules
windowrulev2 = float, class:^(Trezor Suite)$
windowrulev2 = size 40% 65%, class:^(Trezor Suite)$
windowrulev2 = center, class:^(Trezor Suite)$

# Ledger Live window rules
windowrulev2 = float, class:^(Ledger Live)$
windowrulev2 = size 50% 85%, class:^(Ledger Live)$
windowrulev2 = center, class:^(Ledger Live)$
windowrulev2 = float, class:^(ledger live)$
windowrulev2 = size 50% 85%, class:^(ledger live)$
windowrulev2 = center, class:^(ledger live)$

# Lollypop music player window rules
windowrulev2 = float, class:^(org.gnome.Lollypop)$
windowrulev2 = size 50% 85%, class:^(org.gnome.Lollypop)$
windowrulev2 = center, class:^(org.gnome.Lollypop)$

# MPV media player window rules
windowrulev2 = float, class:^(mpv)$
windowrulev2 = size 75% 75%, class:^(mpv)$
windowrulev2 = center, class:^(mpv)$

# Fragments window rules
windowrulev2 = float, class:^(de.haeckerfelix.Fragments)$
windowrulev2 = size 35% 50%, class:^(de.haeckerfelix.Fragments)$
windowrulev2 = center, class:^(de.haeckerfelix.Fragments)$

# Gnome System Monitor window rules
windowrulev2 = float, class:^(gnome-system-monitor)$
windowrulev2 = size 1000 85%, class:^(gnome-system-monitor)$
windowrulev2 = center, class:^(gnome-system-monitor)$
windowrulev2 = float, class:^(org.gnome.SystemMonitor)$
windowrulev2 = size 1000 85%, class:^(org.gnome.SystemMonitor)$
windowrulev2 = center, class:^(org.gnome.SystemMonitor)$

# iwgtk WiFi manager window rules
windowrulev2 = float, class:^(org.twosheds.iwgtk)$
windowrulev2 = size 600 85%, class:^(org.twosheds.iwgtk)$
windowrulev2 = center, class:^(org.twosheds.iwgtk)$

# nwg-displays monitor configuration window rules
windowrulev2 = float, class:^(nwg-displays)$
windowrulev2 = size 900 85%, class:^(nwg-displays)$
windowrulev2 = center, class:^(nwg-displays)$

# Gnome Disk Utility window rules
windowrulev2 = float, class:^(org.gnome.DiskUtility)$
windowrulev2 = size 800 600, class:^(org.gnome.DiskUtility)$
windowrulev2 = center, class:^(org.gnome.DiskUtility)$

# Gnome Text Editor window rules
windowrulev2 = opacity 0.96 0.94, class:^(org\.gnome\.TextEditor)$

# ArchRiot Welcome window rules
windowrulev2 = float, title:^(Welcome to ArchRiot)$
windowrulev2 = center, title:^(Welcome to ArchRiot)$
windowrulev2 = size 800 85%, title:^(Welcome to ArchRiot)$

# ArchRiot Help System window rules
# Keybinding Help PWA (generated by script)
windowrulev2 = float, class:^(brave-.*\.cache_archriot_help.*)$
windowrulev2 = noborder, class:^(brave-.*\.cache_archriot_help.*)$
windowrulev2 = noshadow, class:^(brave-.*\.cache_archriot_help.*)$
windowrulev2 = size 600 70%, class:^(brave-.*\.cache_archriot_help.*)$
windowrulev2 = opacity 0.85 0.85, class:^(brave-.*\.cache_archriot_help.*)$
# Also match early/initial spawn states
windowrulev2 = float, initialClass:^(brave-.*\.cache_archriot_help.*)$
windowrulev2 = noborder, initialClass:^(brave-.*\.cache_archriot_help.*)$
windowrulev2 = noshadow, initialClass:^(brave-.*\.cache_archriot_help.*)$

# ArchRiot Version Update dialog window rules
windowrulev2 = float, title:^(ArchRiot Update Available)$
windowrulev2 = center, title:^(ArchRiot Update Available)$
windowrulev2 = size 600 85%, title:^(ArchRiot Update Available)$

# ArchRiot Control Panel window rules
windowrulev2 = float, title:^(ArchRiot Control Panel)$
windowrulev2 = center, title:^(ArchRiot Control Panel)$
windowrulev2 = size 900 85%, title:^(ArchRiot Control Panel)$

# Floating terminal window rules
# windowrulev2 = float, class:^(com.mitchellh.ghostty-floating)$
# windowrulev2 = center, class:^(com.mitchellh.ghostty-floating)$
# windowrulev2 = size 900 80%, class:^(com.mitchellh.ghostty-floating)$

windowrulev2 = size 52% 85%, floating:1, class:^(brave-.*)$

# Subtle window transparency (98% opacity, 2% background visible)
windowrulev2 = opacity 0.96 0.94, class:^(com.mitchellh.ghostty)$
windowrulev2 = opacity 0.96 0.94, class:^(com.mitchellh.ghostty-floating)$
windowrulev2 = opacity 0.96 0.94, class:^(foot)$
windowrulev2 = opacity 0.96 0.94, class:^(Code)$
windowrulev2 = opacity 0.96 0.94, class:^(code-oss)$
windowrulev2 = opacity 0.96 0.94, class:^(VSCodium)$
windowrulev2 = opacity 0.96 0.94, class:^(neovim)$
windowrulev2 = opacity 1.00 0.98, class:^(Thunar)$
windowrulev2 = opacity 0.96 0.94, class:^(org.gnome.TextEditor)$
windowrulev2 = opacity 0.96 0.94, class:^(Signal)$
windowrulev2 = opacity 0.96 0.94, class:^(signal)$
windowrulev2 = opacity 0.96 0.94, class:^(org\.telegram\.desktop)$
windowrulev2 = opacity 0.96 0.94, class:^(telegram-desktop)$

# Pop-up windows (modals) that tend to open in fullscreen
windowrulev2 = float, class:^(xdg-desktop-portal-gtk)$, title:^(Save a File)$
windowrulev2 = center, class:^(Electron)$, title:^(Code - OSS)$
windowrulev2 = float, class:^(Thunar)$, title:^(Rename .*)$
windowrulev2 = float, class:^(Thunar)$, title:^(File Operation Progress)$
windowrulev2 = float, title:^(.*wants to save*.)$

# ----------------------------------------------------------------------------
# Change your personal window rules in here to keep the main config portable
source = ~/.config/hypr/windowrules.conf
# Do not add window rules after this line.
# ----------------------------------------------------------------------------

# ===================== INPUT CONFIGURATION ====================
# Inputs
input {
    kb_layout = us
    kb_variant = intl
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1
    sensitivity = 0

    repeat_rate = 40
    repeat_delay = 600

    # touchpad {
    #     natural_scroll = false
    #     scroll_factor = 0.4
    #     clickfinger_behavior = false      # Traditional corner/button right-click
    #     tap_button_map = lrm             # Left, Right, Middle click with 1, 2, 3 fingers
    #     disable_while_typing = true      # Prevent accidental clicks while typing
    #     drag_lock = false                # Disable drag lock for better experience
    #     tap_to_click = true              # Enable tap-to-click
    #     middle_button_emulation = true   # Emulate middle click with both buttons
    # }
}

# ========================== APPEARANCE ========================
# General appearance
general {
    gaps_in = 0
    gaps_out = 2
    border_size = 1
    col.active_border = rgba(89b4fa88) 45deg
    col.inactive_border = rgba(1a1a1a60)
    layout = dwindle
    allow_tearing = false
}

# Decorations
decoration {
    rounding = 8

    active_opacity = 1.0
    inactive_opacity = 0.95
    fullscreen_opacity = 1.0

    dim_inactive = true
    dim_strength = 0.05
    dim_special = 0.5

    blur {
        enabled = true
        size = 3
        passes = 2
        ignore_opacity = true
        new_optimizations = true
        special = false
        popups = false
    }

    shadow {
        enabled = true
        range = 4
        render_power = 2
        color = rgba(1a1b26cc)
        color_inactive = rgba(1a1b26aa)
    }
}

# Animations
animations {
    enabled = true

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    # animation = NAME, ONOFF, SPEED, CURVE [,STYLE]
    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

# Layouts
dwindle {
    pseudotile = true        # tile floating windows
    preserve_split = true    # dont change split orientation on resize
    smart_split = false      # split orientation based on cursor position
    default_split_ratio = 1  # 1=center(50/50), lower=bigger_left, higher=bigger_right
}

master {
    new_status = master
}

# Miscellaneous
misc {
    force_default_wallpaper = 0
    disable_hyprland_logo = true
    animate_manual_resizes = false
}

# Group styling
group {
    col.border_active = rgba(89b4fa88) 45deg

    groupbar {
        col.active = rgba(bb9af7ff)
    }
}

# ========================= KEYBINDINGS ========================
# Start default apps
bind = $mod, return, exec, $terminal                                           # Default Terminal
bind = $mod, slash, exec, $terminal                                            # Default Terminal (alt-keys)
bind = $mod SHIFT, return, exec, $terminal --class=com.mitchellh.ghostty-floating  # Default Terminal (flying)
bind = $mod, F, exec, $fileManager                                             # File Manager
bind = $mod, B, exec, $browser                                                 # Browser
bind = $mod, N, exec, $terminal -e nvim                                        # NeoVim
bind = $mod, T, exec, $terminal --title=btop -e btop                           # System Monitor
bind = $mod, G, exec, sh -lc 'notify-send -t 2000 "Telegram" "Opening Telegram..." >/dev/null 2>&1; Telegram'  # Telegram
bind = $mod, S, exec, $HOME/.local/share/archriot/install/archriot --signal    # Signal
bind = $mod, O, exec, gnome-text-editor                                        # Text Editor
bind = $mod, C, exec, $HOME/.local/share/archriot/config/bin/archriot-control-panel    # Control Panel
bind = $mod, Z, exec, zeditor                                                  # Zed
bind = $mod, R, exec, $HOME/.local/share/archriot/install/archriot --wallet    # Wallet

# App launcher
bind = $mod, D, exec, fuzzel     # App Launcher
bind = $mod, space, exec, fuzzel # App Launcher (alt-keys)

# App closer
bind = $mod, W, killactive,     # Close active window
bind = $mod, Q, killactive,     # Close active window (alt-keys)

# Control Panel
bind = $mod, C, exec, notify-send "Opening Control Panel..." && DISPLAY=:0 $HOME/.local/share/archriot/config/bin/archriot-control-panel # ArchRiot Control Panel

# Pomodoro Timer
bind = $mod, comma, exec, $HOME/.local/share/archriot/install/archriot --pomodoro-click  # Pomodoro Timer

# Help system
bind = $mod, H, exec, $browser --class=archriot-help --app="https://archriot.org"    # ArchRiot Help System
bind = $mod SHIFT, H, exec, $HOME/.local/share/archriot/install/archriot --help-binds-web  # Keybindings Mapping

# Waybar and theme controls
bind = $mod SHIFT, SPACE, exec, $HOME/.local/share/archriot/install/archriot --waybar-reload  # Reload Waybar
bind = $mod CTRL, SPACE, exec, $HOME/.local/share/archriot/install/archriot --swaybg-next     # Cycle Desktop Background

# Notifications
bind = $mod, period, exec, makoctl dismiss              # Dismiss notification
bind = $mod SHIFT, period, exec, makoctl dismiss --all  # Dismiss all notifications

# ======================= WINDOW BINDINGS ======================
# Window management
bind = $mod, V, togglefloating, # Toggle Floating
bind = $mod, J, togglesplit,    # Toggle Split
bind = $mod, P, pseudo,         # Pseudo Tiling
bind = $mod, TAB, exec, $HOME/.local/share/archriot/install/archriot --switch-window  # Window Switcher
bind = ALT, TAB, exec, hyprctl dispatch cyclenext visible                             # Cycle next windows
bind = ALT SHIFT, TAB, exec, hyprctl dispatch cyclenext prev visible                  # Cycle previous windows

# Fix off-screen windows (manual trigger)
bind = $mod SHIFT, TAB, exec, $HOME/.local/share/archriot/install/archriot --fix-offscreen-windows  # Fix offscreen windows

# Move focus
bind = $mod, left, movefocus, l   # Move focus left
bind = $mod, right, movefocus, r  # Move focus right
bind = $mod, up, movefocus, u     # Move focus up
bind = $mod, down, movefocus, d   # Move focus down

# Switch workspaces
bind = $mod, 1, workspace, 1  # Move to Workspace 1
bind = $mod, 2, workspace, 2  # Move to Workspace 2
bind = $mod, 3, workspace, 3  # Move to Workspace 3
bind = $mod, 4, workspace, 4  # Move to Workspace 4
bind = $mod, 5, workspace, 5  # Move to Workspace 5
bind = $mod, 6, workspace, 6  # Move to Workspace 6

# Move windows to workspaces
bind = $mod SHIFT, 1, movetoworkspace, 1  # Move window to Workspace 1
bind = $mod SHIFT, 2, movetoworkspace, 2  # Move window to Workspace 2
bind = $mod SHIFT, 3, movetoworkspace, 3  # Move window to Workspace 3
bind = $mod SHIFT, 4, movetoworkspace, 4  # Move window to Workspace 4
bind = $mod SHIFT, 5, movetoworkspace, 5  # Move window to Workspace 5
bind = $mod SHIFT, 6, movetoworkspace, 6  # Move window to Workspace 6

# Swap windows
bind = $mod SHIFT, left, swapwindow, l   # Swap windows left
bind = $mod SHIFT, right, swapwindow, r  # Swap windows right
bind = $mod SHIFT, up, swapwindow, u     # Swap windows up
bind = $mod SHIFT, down, swapwindow, d   # Swap windows down

# Move windows around (smart movement)
bind = $mod CTRL, left, movewindow, l   # Smart-move windows left
bind = $mod CTRL, right, movewindow, r  # Smart-move windows right
bind = $mod CTRL, up, movewindow, u     # Smart-move windows up
bind = $mod CTRL, down, movewindow, d   # Smart-move windows down

# Resize windows
bind = $mod, minus, resizeactive, -80 0        # Resize windows left
bind = $mod, equal, resizeactive, 80 0         # Resize windows right
bind = $mod SHIFT, minus, resizeactive, 0 -80  # Resize windows up
bind = $mod SHIFT, equal, resizeactive, 0 80   # Resize windows down

# Mouse bindings
bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow
bind = $mod, mouse_down, workspace, e+1  # Next Workspace
bind = $mod, mouse_up, workspace, e-1    # Previous Workspace

# ======================= MEDIA BINDINGS =======================
# Media keys
bindel = ,XF86AudioRaiseVolume, exec, $HOME/.local/share/archriot/install/archriot --volume inc
bindel = ,XF86AudioLowerVolume, exec, $HOME/.local/share/archriot/install/archriot --volume dec
bindel = ,XF86AudioMute, exec, $HOME/.local/share/archriot/install/archriot --volume toggle
bindel = ,XF86AudioMicMute, exec, $HOME/.local/share/archriot/install/archriot --volume mic-toggle
bindel = ,XF86MonBrightnessUp, exec, $HOME/.local/share/archriot/install/archriot --brightness up
bindel = ,XF86MonBrightnessDown, exec, $HOME/.local/share/archriot/install/archriot --brightness down

# Media controls
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# Screenshots
bind = , PRINT, exec, hyprshot -m region       # Screenshot region
bind = SHIFT, PRINT, exec, hyprshot -m window  # Screenshot window
bind = CTRL, PRINT, exec, hyprshot -m output   # Screenshot output
bind = $mod SHIFT, S, exec, hyprshot -m region # Screenshot region (alt-keys)
bind = $mod SHIFT, W, exec, hyprshot -m window # Screenshot window (alt-keys)
bind = $mod SHIFT, F, exec, hyprshot -m output # Screenshot output (alt-keys)

# Color picker
bind = $mod, PRINT, exec, hyprpicker -a  # Color Picker

# Mail key binding
bind = , XF86Mail, exec, $browser --app="https://mail.proton.me/u/11/inbox"

# ======================= WEB APP BINDINGS =====================
bind = $mod, E, exec, $browser --app="https://mail.proton.me/u/11/inbox"  # Proton Mail
bind = $mod, M, exec, $browser --app="https://messages.google.com/web/conversations"  # Google Messages
bind = $mod, X, exec, $browser --app="https://x.com/"                     # X (Twitter)
bind = $mod SHIFT, X, exec, $browser --app="https://x.com/compose/post"   # Post on X (Twitter)

# ======================== SYSTEM CONTROLS =====================
# Lock screen
bind = $mod, L, exec, hyprlock  # Lock Screen

# Power menu
bind = $mod, ESCAPE, exec, $HOME/.local/share/archriot/install/archriot --power-menu  # Power Menu


# ----------------------------------------------------------------------------
# Change your personal keybindings in here to keep the main config portable
source = ~/.config/hypr/keybindings.conf
# Do not add new keybindings after this line.
# ----------------------------------------------------------------------------

# ===================== MONITOR EVENT HOOKS ====================
# Removed automatic window fixing to prevent unwanted repositioning

# ======================= THEME & STYLING ======================

# ================ PERFORMANCE BALANCED PRESET =================
# Keep things snappy on Intel Core Ultra iGPU: short animations, no blur, lighter shadows
# These override earlier values (Hyprland uses last-wins semantics)

# decoration {
#     inactive_opacity = 1.0

#     blur {
#         enabled = false
#     }

#     shadow {
#         enabled = true
#         range = 3
#         render_power = 1
#     }
# }

animations {
    enabled = true

    # keep existing bezier but use shorter timings
    animation = windows, 1, 4, myBezier
    animation = windowsOut, 1, 4, default, popin 70%
    animation = border, 1, 6, default
    animation = borderangle, 1, 5, default
    animation = fade, 1, 4, default
    animation = workspaces, 1, 3, default
}
